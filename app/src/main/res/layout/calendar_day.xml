<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:minHeight="120dp"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Día del calendario (Número del día) -->
        <TextView
            android:id="@+id/calendarDayText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/black"
            android:textSize="16sp" />

        <!-- Contenedor para los eventos -->
        <LinearLayout
            android:id="@+id/eventContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Texto del primer evento -->
            <TextView
                android:id="@+id/eventTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#76387c"
                android:textSize="12sp"
                android:visibility="gone" />

            <!-- Aquí añadirás más eventos, si es necesario -->

            <!-- Indicador de más eventos (solo visible si hay más de un evento) -->
            <TextView
                android:id="@+id/eventPlusTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="+"
                android:textSize="12sp"
                android:visibility="gone"
                android:textColor="#76387c" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
